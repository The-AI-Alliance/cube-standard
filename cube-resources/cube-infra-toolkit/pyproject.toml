[project]
name = "cube-infra-toolkit"
version = "0.1.0"
description = "EAI Toolkit InfraConfig for CUBE resource lifecycle — per-task job sandboxes"
authors = [
    { name = "Alexandre Lacoste", email = "alexandre.lacoste@servicenow.com" },
]
requires-python = ">=3.12"
dependencies = [
    "cube-standard",
    "tenacity>=8.5.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest>=9.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/cube_infra_toolkit"]

[tool.uv.sources]
cube-standard = { workspace = true }
