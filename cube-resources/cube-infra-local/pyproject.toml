[project]
name = "cube-infra-local"
version = "0.1.0"
description = "Local QEMU/Docker InfraConfig implementation for CUBE"
requires-python = ">=3.12"
dependencies = [
    "cube-standard",
    "requests>=2.28",
    "tqdm>=4.60",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
]

[build-system]
requires = ["uv_build>=0.6.0,<0.7.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "cube_infra_local"

[tool.ruff]
fix = true
line-length = 120
indent-width = 4

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"

[tool.ruff.lint]
extend-select = ["I"]
